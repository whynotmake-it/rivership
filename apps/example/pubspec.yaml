name: example
description: "A new Flutter project."

# Prevent accidental publishing to pub.dev.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  cupertino_icons: ^1.0.8
  figma_squircle_updated: ^1.0.1
  flutter:
    sdk: flutter
  heroine_example:
    path: ../../packages/heroine/example
  springster_example:
    path: ../../packages/springster/example


  rivership: ^0.2.0+1
  go_router: ^14.6.3

  

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

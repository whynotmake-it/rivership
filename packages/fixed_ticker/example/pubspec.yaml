name: fixed_ticker_example
description: Example app demonstrating FixedTicker with motor animations.
publish_to: none
version: 1.0.0
resolution: workspace

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  fixed_ticker:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

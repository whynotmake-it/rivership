include: package:lintervention/analysis_options.yaml

analyzer:
  errors:
    # This package is the first-party consumer of motor's experimental
    # inspection API and moves in lockstep with it.
    experimental_member_use: ignore

import 'package:flutter_test/flutter_test.dart';
import 'package:rivership/rivership.dart';

void main() {
  group('AnimatedSizeSwitcher', () {
    setUp(() {});

    test('is exported by package', () async {
      expect(AnimatedSizeSwitcher, isA<Type>());
    });
  });
}

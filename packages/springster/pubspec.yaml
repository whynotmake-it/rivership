name: springster
description: Spring animations and simulations, simplified.
version: 0.1.0


environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ">=0.20.5 <0.21.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  lintervention: ^0.1.1
  
  mocktail: ^1.0.3

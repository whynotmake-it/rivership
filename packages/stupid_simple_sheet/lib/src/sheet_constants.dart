/// Tolerance used when comparing normalized sheet positions.
///
/// Positions within this distance of zero are treated as closed, and
/// positions within this distance of a snap point are treated as settled.
const double sheetPositionTolerance = 0.001;
